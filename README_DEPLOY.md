# Team 7 Stress Detection App — V6

Modern Streamlit dashboard with prediction, validation check, local SHAP explanation, and smart wellness recommendation cards.

```bash
cp ~/Documents/df_windows.csv .
conda activate streamlit_week4
pip install -r requirements.txt
python train_and_export_model.py
streamlit run app.py
```
